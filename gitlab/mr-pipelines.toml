description = "List pipelines associated with a GitLab merge request."
prompt = """
User arguments: "{{args}}"
1. Parse arguments as "<project_id> <merge_request_iid>".
2. If values are missing, ask the user to rerun with both fields.
3. Call the `gitlab.get_merge_request_pipelines_service` tool with:
   {
     "project_id": "<project_id>",
     "merge_request_iid": "<merge_request_iid>"
   }
4. Summarize pipeline IDs, statuses, sources, and most recent run times, noting failed or pending jobs that need attention.
"""
