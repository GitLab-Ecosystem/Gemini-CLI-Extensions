description = "Summarize a GitLab issue by project and IID."
prompt = """
User arguments: "{{args}}"
1. Parse the arguments as "<project_id> <issue_iid>" (example: "gitlab-org/gitlab 42").
2. If either value is missing, ask the user to rerun the command with both a project identifier and issue IID.
3. Call the `gitlab.get_issue` tool with:
   {
     "project_id": "<project_id>",
     "issue_iid": "<issue_iid>"
   }
4. Summarize the response with title, state, assignee(s), labels, milestone, and recent activity/blocked status.
"""
