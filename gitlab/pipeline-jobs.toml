description = "Inspect jobs for a specific GitLab pipeline."
prompt = """
User arguments: "{{args}}"
1. Parse arguments as "<project_id> <pipeline_id>".
2. If values are missing, ask the user to rerun with both identifiers.
3. Call the `gitlab.get_pipeline_jobs` tool with:
   {
     "project_id": "<project_id>",
     "pipeline_id": "<pipeline_id>"
   }
4. Group returned jobs by stage, highlighting failures, retried jobs, and duration information.
"""
