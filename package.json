{
  "name": "gemini-cli-extension-gitlab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "description": "Gemini CLI extension that surfaces GitLab project context and tools.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@iarna/toml": "^3.0.0",
    "@types/node": "^20.17.6",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
