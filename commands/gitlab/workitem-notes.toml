description = "Get all comments (notes) for a specific work item."
prompt = """
User arguments: "{{args}}"
1. Parse the user arguments to identify the target work item (either via `url` OR `project_id` and `work_item_iid`).
2. Ask the user for missing details if necessary.
3. Call the `gitlab.get_workitem_notes` tool.
4. Summarize the discussions/notes on the work item.
"""
