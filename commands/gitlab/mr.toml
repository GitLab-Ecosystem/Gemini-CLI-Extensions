description = "Inspect a GitLab merge request by project and IID."
prompt = """
User arguments: "{{args}}"
1. Parse arguments as "<project_id> <merge_request_iid>".
2. If either value is missing, ask the user to rerun the command with both pieces of information.
3. Call the `gitlab.get_merge_request` tool with:
   {
     "project_id": "<project_id>",
     "merge_request_iid": "<merge_request_iid>"
   }
4. Summarize title, author, state, reviewers, latest pipeline status, and outstanding discussions.
"""
