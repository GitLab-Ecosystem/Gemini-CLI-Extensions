description = "Manage CI/CD pipelines in a GitLab project."
prompt = """
User arguments: "{{args}}"
1. Parse the user arguments to determine the action to take. Actions can include list, create, update, retry, cancel, or delete a pipeline.
2. Ensure you have the `id` (project id). For actions like cancel, retry, you will also need the `pipeline_id`.
3. Call the `gitlab.manage_pipeline` tool with the appropriate operations.
4. Summarize the result of the pipeline management operation.
"""
